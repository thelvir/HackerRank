#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    char first[1000];
    char second[10000];
    char thirst[1000];
    scanf("%s\n",&first);
    scanf("%s\n",&second);
    gets(thirst);
    puts(first);
    puts(second);
    puts(thirst);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
